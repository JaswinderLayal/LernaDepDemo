{
  "scripts": {
    "bootstrap": "npx lerna bootstrap --hoist",
    "test": "jest",
    "publish": "npx lerna publish --skip-git --exact",
    "commit": "npx git-cz"
  },
  "devDependencies": {
    "jest": "^29.3.1",
    "lerna": "^2.9.0"
  },
  "dependencies": {
    "commitizen": "^4.0.4"
  },
  "config": {
    "commitizen": {
      "path": "cz-conventional-changelog"
    }
  }
}
